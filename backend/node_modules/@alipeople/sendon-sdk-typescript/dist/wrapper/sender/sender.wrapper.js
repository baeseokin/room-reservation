"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SenderService = void 0;
/* eslint-disable no-console */
const rest_1 = require("../../rest");
class SenderService {
    constructor(configuration, debug) {
        this.senderClient = new rest_1.SdoSender(configuration);
        this.debug = debug;
    }
    async listUserNumbers(page, limit) {
        const response = await this.senderClient.listUserNumbers(page, limit);
        if (this.debug)
            console.log(`response: ${JSON.stringify(response.data, null, 2)}`);
        return response.data;
    }
}
exports.SenderService = SenderService;
