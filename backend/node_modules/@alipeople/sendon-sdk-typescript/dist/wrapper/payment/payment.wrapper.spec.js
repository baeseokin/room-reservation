"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const config_1 = require("../config");
const payment_wrapper_1 = require("./payment.wrapper");
describe('payment', () => {
    let service;
    beforeAll(async () => {
        service = new payment_wrapper_1.PaymentService({
            username: config_1.USERNAME,
            password: config_1.PASSWORD,
            basePath: config_1.BASEPATH,
        }, true);
    });
    it('getPaymentHistories', async () => {
        const response = await service.getPaymentHistories({
            page: 1,
            limit: 10,
            startDate: new Date('2024-01-01'),
            endDate: new Date('2024-02-29'),
        });
        expect(response).toBeDefined();
    });
    it('getPaymentHistoryDetail', async () => {
        const response = await service.getPaymentHistoryDetail(1);
        expect(response).toBeDefined();
    });
});
