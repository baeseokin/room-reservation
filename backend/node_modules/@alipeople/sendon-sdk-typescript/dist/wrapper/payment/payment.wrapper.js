"use strict";
/* eslint-disable no-console */
Object.defineProperty(exports, "__esModule", { value: true });
exports.PaymentService = void 0;
const rest_1 = require("../../rest");
class PaymentService {
    constructor(configuration, debug) {
        this.paymentClient = new rest_1.SdoPayment(configuration);
        this.debug = debug;
    }
    async getPaymentHistories(params) {
        var _a, _b;
        const response = await this.paymentClient.getPaymentHistories(params.page, params.limit, (_a = params.startDate) === null || _a === void 0 ? void 0 : _a.toISOString(), (_b = params.endDate) === null || _b === void 0 ? void 0 : _b.toISOString());
        if (this.debug)
            console.log(`response: ${JSON.stringify(response.data, null, 2)}`);
        return response.data;
    }
    async getPaymentHistoryDetail(historyId) {
        const response = await this.paymentClient.getPaymentHistory(historyId);
        if (this.debug)
            console.log(`response: ${JSON.stringify(response.data, null, 2)}`);
        return response.data;
    }
}
exports.PaymentService = PaymentService;
