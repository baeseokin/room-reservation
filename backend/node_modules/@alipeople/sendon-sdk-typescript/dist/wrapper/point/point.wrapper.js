"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PointService = void 0;
/* eslint-disable no-console */
const rest_1 = require("../../rest");
class PointService {
    constructor(configuration, debug) {
        this.pointClient = new rest_1.SdoPoint(configuration);
        this.debug = debug;
    }
    async getCost() {
        const response = await this.pointClient.getCost();
        if (this.debug)
            console.log(`response: ${JSON.stringify(response.data, null, 2)}`);
        return response.data;
    }
    async listPoints() {
        const response = await this.pointClient.listPoints();
        if (this.debug)
            console.log(`response: ${JSON.stringify(response.data, null, 2)}`);
        return response.data;
    }
}
exports.PointService = PointService;
