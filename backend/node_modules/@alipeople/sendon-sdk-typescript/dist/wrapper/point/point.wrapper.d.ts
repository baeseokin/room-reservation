import { Configuration, CostGetResponseDto, PointsListResponseDto } from '../../rest';
export type SdoCostGetResponse = CostGetResponseDto;
export type SdoPointsListResponse = PointsListResponseDto;
export declare class PointService {
    private pointClient;
    private debug;
    constructor(configuration: Configuration, debug: boolean);
    getCost(): Promise<SdoCostGetResponse>;
    listPoints(): Promise<SdoPointsListResponse>;
}
