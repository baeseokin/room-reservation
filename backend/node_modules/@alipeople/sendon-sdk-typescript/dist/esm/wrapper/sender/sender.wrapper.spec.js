import { BASEPATH, PASSWORD, USERNAME } from '../config';
import { SenderService } from './sender.wrapper';
describe('sms', () => {
    let service;
    beforeAll(async () => {
        service = new SenderService({
            username: USERNAME,
            password: PASSWORD,
            basePath: BASEPATH,
        }, true);
    });
});
