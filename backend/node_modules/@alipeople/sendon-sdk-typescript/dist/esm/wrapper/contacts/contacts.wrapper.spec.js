import { BASEPATH, PASSWORD, USERNAME } from '../config';
import { ContactsService } from './contacts.wrapper';
describe('sms', () => {
    let service;
    beforeAll(async () => {
        service = new ContactsService({
            username: USERNAME,
            password: PASSWORD,
            basePath: BASEPATH,
        }, true);
    });
});
