import { BASEPATH, PASSWORD, USERNAME } from '../config';
import { RcsService } from './rcs.wrapper';
describe('sms', () => {
    let service;
    beforeAll(async () => {
        service = new RcsService({
            username: USERNAME,
            password: PASSWORD,
            basePath: BASEPATH,
        }, true);
    });
    it(`rcs.send`, () => { });
    it(`rcs.find`, () => { });
});
