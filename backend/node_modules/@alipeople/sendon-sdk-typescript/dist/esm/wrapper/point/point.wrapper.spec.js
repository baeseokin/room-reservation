import { USERNAME, PASSWORD, BASEPATH } from '../config';
import { PointService } from './point.wrapper';
describe('sms', () => {
    let service;
    beforeAll(async () => {
        service = new PointService({
            username: USERNAME,
            password: PASSWORD,
            basePath: BASEPATH,
        }, true);
    });
});
